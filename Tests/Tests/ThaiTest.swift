// Note: This file is automatically generated by the python script Tests/testsFromGroundtruth.py
//       Running this script will be overwrite any changes you make to this file

import Testing
@testable import NumberTranslator

@Test func Thai() {
    let translator = NumberTranslator()
    #expect(translator.translate(0, to: .thai).x == "ศูนย์")
    #expect(translator.translate(1, to: .thai).x == "หนึ่ง")
    #expect(translator.translate(2, to: .thai).x == "สอง")
    #expect(translator.translate(3, to: .thai).x == "สาม")
    #expect(translator.translate(4, to: .thai).x == "สี่")
    #expect(translator.translate(5, to: .thai).x == "ห้า")
    #expect(translator.translate(6, to: .thai).x == "หก")
    #expect(translator.translate(7, to: .thai).x == "เจ็ด")
    #expect(translator.translate(8, to: .thai).x == "แปด")
    #expect(translator.translate(9, to: .thai).x == "เก้า")
    #expect(translator.translate(10, to: .thai).x == "สิบ")
    #expect(translator.translate(11, to: .thai).x == "สิบเอ็ด")
    #expect(translator.translate(12, to: .thai).x == "สิบสอง")
    #expect(translator.translate(13, to: .thai).x == "สิบสาม")
    #expect(translator.translate(14, to: .thai).x == "สิบสี่")
    #expect(translator.translate(15, to: .thai).x == "สิบห้า")
    #expect(translator.translate(16, to: .thai).x == "สิบหก")
    #expect(translator.translate(17, to: .thai).x == "สิบเจ็ด")
    #expect(translator.translate(18, to: .thai).x == "สิบแปด")
    #expect(translator.translate(19, to: .thai).x == "สิบเก้า")
    #expect(translator.translate(20, to: .thai).x == "ยี่สิบ")
    #expect(translator.translate(21, to: .thai).x == "ยี่สิบเอ็ด")
    #expect(translator.translate(22, to: .thai).x == "ยี่สิบสอง")
    #expect(translator.translate(23, to: .thai).x == "ยี่สิบสาม")
    #expect(translator.translate(24, to: .thai).x == "ยี่สิบสี่")
    #expect(translator.translate(25, to: .thai).x == "ยี่สิบห้า")
    #expect(translator.translate(26, to: .thai).x == "ยี่สิบหก")
    #expect(translator.translate(27, to: .thai).x == "ยี่สิบเจ็ด")
    #expect(translator.translate(28, to: .thai).x == "ยี่สิบแปด")
    #expect(translator.translate(29, to: .thai).x == "ยี่สิบเก้า")
    #expect(translator.translate(30, to: .thai).x == "สามสิบ")
    #expect(translator.translate(31, to: .thai).x == "สามสิบเอ็ด")
    #expect(translator.translate(32, to: .thai).x == "สามสิบสอง")
    #expect(translator.translate(33, to: .thai).x == "สามสิบสาม")
    #expect(translator.translate(34, to: .thai).x == "สามสิบสี่")
    #expect(translator.translate(35, to: .thai).x == "สามสิบห้า")
    #expect(translator.translate(36, to: .thai).x == "สามสิบหก")
    #expect(translator.translate(37, to: .thai).x == "สามสิบเจ็ด")
    #expect(translator.translate(38, to: .thai).x == "สามสิบแปด")
    #expect(translator.translate(39, to: .thai).x == "สามสิบเก้า")
    #expect(translator.translate(40, to: .thai).x == "สี่สิบ")
    #expect(translator.translate(41, to: .thai).x == "สี่สิบเอ็ด")
    #expect(translator.translate(42, to: .thai).x == "สี่สิบสอง")
    #expect(translator.translate(43, to: .thai).x == "สี่สิบสาม")
    #expect(translator.translate(44, to: .thai).x == "สี่สิบสี่")
    #expect(translator.translate(45, to: .thai).x == "สี่สิบห้า")
    #expect(translator.translate(46, to: .thai).x == "สี่สิบหก")
    #expect(translator.translate(47, to: .thai).x == "สี่สิบเจ็ด")
    #expect(translator.translate(48, to: .thai).x == "สี่สิบแปด")
    #expect(translator.translate(49, to: .thai).x == "สี่สิบเก้า")
    #expect(translator.translate(50, to: .thai).x == "ห้าสิบ")
    #expect(translator.translate(51, to: .thai).x == "ห้าสิบเอ็ด")
    #expect(translator.translate(52, to: .thai).x == "ห้าสิบสอง")
    #expect(translator.translate(53, to: .thai).x == "ห้าสิบสาม")
    #expect(translator.translate(54, to: .thai).x == "ห้าสิบสี่")
    #expect(translator.translate(55, to: .thai).x == "ห้าสิบห้า")
    #expect(translator.translate(56, to: .thai).x == "ห้าสิบหก")
    #expect(translator.translate(57, to: .thai).x == "ห้าสิบเจ็ด")
    #expect(translator.translate(58, to: .thai).x == "ห้าสิบแปด")
    #expect(translator.translate(59, to: .thai).x == "ห้าสิบเก้า")
    #expect(translator.translate(60, to: .thai).x == "หกสิบ")
    #expect(translator.translate(61, to: .thai).x == "หกสิบเอ็ด")
    #expect(translator.translate(62, to: .thai).x == "หกสิบสอง")
    #expect(translator.translate(63, to: .thai).x == "หกสิบสาม")
    #expect(translator.translate(64, to: .thai).x == "หกสิบสี่")
    #expect(translator.translate(65, to: .thai).x == "หกสิบห้า")
    #expect(translator.translate(66, to: .thai).x == "หกสิบหก")
    #expect(translator.translate(67, to: .thai).x == "หกสิบเจ็ด")
    #expect(translator.translate(68, to: .thai).x == "หกสิบแปด")
    #expect(translator.translate(69, to: .thai).x == "หกสิบเก้า")
    #expect(translator.translate(70, to: .thai).x == "เจ็ดสิบ")
    #expect(translator.translate(71, to: .thai).x == "เจ็ดสิบเอ็ด")
    #expect(translator.translate(72, to: .thai).x == "เจ็ดสิบสอง")
    #expect(translator.translate(73, to: .thai).x == "เจ็ดสิบสาม")
    #expect(translator.translate(74, to: .thai).x == "เจ็ดสิบสี่")
    #expect(translator.translate(75, to: .thai).x == "เจ็ดสิบห้า")
    #expect(translator.translate(76, to: .thai).x == "เจ็ดสิบหก")
    #expect(translator.translate(77, to: .thai).x == "เจ็ดสิบเจ็ด")
    #expect(translator.translate(78, to: .thai).x == "เจ็ดสิบแปด")
    #expect(translator.translate(79, to: .thai).x == "เจ็ดสิบเก้า")
    #expect(translator.translate(80, to: .thai).x == "แปดสิบ")
    #expect(translator.translate(81, to: .thai).x == "แปดสิบเอ็ด")
    #expect(translator.translate(82, to: .thai).x == "แปดสิบสอง")
    #expect(translator.translate(83, to: .thai).x == "แปดสิบสาม")
    #expect(translator.translate(84, to: .thai).x == "แปดสิบสี่")
    #expect(translator.translate(85, to: .thai).x == "แปดสิบห้า")
    #expect(translator.translate(86, to: .thai).x == "แปดสิบหก")
    #expect(translator.translate(87, to: .thai).x == "แปดสิบเจ็ด")
    #expect(translator.translate(88, to: .thai).x == "แปดสิบแปด")
    #expect(translator.translate(89, to: .thai).x == "แปดสิบเก้า")
    #expect(translator.translate(90, to: .thai).x == "เก้าสิบ")
    #expect(translator.translate(91, to: .thai).x == "เก้าสิบเอ็ด")
    #expect(translator.translate(92, to: .thai).x == "เก้าสิบสอง")
    #expect(translator.translate(93, to: .thai).x == "เก้าสิบสาม")
    #expect(translator.translate(94, to: .thai).x == "เก้าสิบสี่")
    #expect(translator.translate(95, to: .thai).x == "เก้าสิบห้า")
    #expect(translator.translate(96, to: .thai).x == "เก้าสิบหก")
    #expect(translator.translate(97, to: .thai).x == "เก้าสิบเจ็ด")
    #expect(translator.translate(98, to: .thai).x == "เก้าสิบแปด")
    #expect(translator.translate(99, to: .thai).x == "เก้าสิบเก้า")
    #expect(translator.translate(100, to: .thai).x == "หนึ่งร้อย")
    #expect(translator.translate(101, to: .thai).x == "หนึ่งร้อยหนึ่ง")
    #expect(translator.translate(102, to: .thai).x == "หนึ่งร้อยสอง")
    #expect(translator.translate(103, to: .thai).x == "หนึ่งร้อยสาม")
    #expect(translator.translate(104, to: .thai).x == "หนึ่งร้อยสี่")
    #expect(translator.translate(105, to: .thai).x == "หนึ่งร้อยห้า")
    #expect(translator.translate(106, to: .thai).x == "หนึ่งร้อยหก")
    #expect(translator.translate(107, to: .thai).x == "หนึ่งร้อยเจ็ด")
    #expect(translator.translate(108, to: .thai).x == "หนึ่งร้อยแปด")
    #expect(translator.translate(109, to: .thai).x == "หนึ่งร้อยเก้า")
    #expect(translator.translate(110, to: .thai).x == "หนึ่งร้อยสิบ")
    #expect(translator.translate(111, to: .thai).x == "หนึ่งร้อยสิบเอ็ด")
    #expect(translator.translate(112, to: .thai).x == "หนึ่งร้อยสิบสอง")
    #expect(translator.translate(113, to: .thai).x == "หนึ่งร้อยสิบสาม")
    #expect(translator.translate(119, to: .thai).x == "หนึ่งร้อยสิบเก้า")
    #expect(translator.translate(120, to: .thai).x == "หนึ่งร้อยยี่สิบ")
    #expect(translator.translate(121, to: .thai).x == "หนึ่งร้อยยี่สิบเอ็ด")
    #expect(translator.translate(126, to: .thai).x == "หนึ่งร้อยยี่สิบหก")
    #expect(translator.translate(190, to: .thai).x == "หนึ่งร้อยเก้าสิบ")
    #expect(translator.translate(200, to: .thai).x == "สองร้อย")
    #expect(translator.translate(201, to: .thai).x == "สองร้อยหนึ่ง")
    #expect(translator.translate(400, to: .thai).x == "สี่ร้อย")
    #expect(translator.translate(444, to: .thai).x == "สี่ร้อยสี่สิบสี่")
    #expect(translator.translate(500, to: .thai).x == "ห้าร้อย")
    #expect(translator.translate(580, to: .thai).x == "ห้าร้อยแปดสิบ")
    #expect(translator.translate(999, to: .thai).x == "เก้าร้อยเก้าสิบเก้า")
    #expect(translator.translate(1_000, to: .thai).x == "หนึ่งพัน")
    #expect(translator.translate(1_001, to: .thai).x == "หนึ่งพันหนึ่ง")
    #expect(translator.translate(1_005, to: .thai).x == "หนึ่งพันห้า")
    #expect(translator.translate(1_009, to: .thai).x == "หนึ่งพันเก้า")
    #expect(translator.translate(1_010, to: .thai).x == "หนึ่งพันสิบ")
    #expect(translator.translate(1_011, to: .thai).x == "หนึ่งพันสิบเอ็ด")
    #expect(translator.translate(1_012, to: .thai).x == "หนึ่งพันสิบสอง")
    #expect(translator.translate(1_015, to: .thai).x == "หนึ่งพันสิบห้า")
    #expect(translator.translate(1_035, to: .thai).x == "หนึ่งพันสามสิบห้า")
    #expect(translator.translate(1_099, to: .thai).x == "หนึ่งพันเก้าสิบเก้า")
    #expect(translator.translate(1_100, to: .thai).x == "หนึ่งพันหนึ่งร้อย")
    #expect(translator.translate(1_101, to: .thai).x == "หนึ่งพันหนึ่งร้อยหนึ่ง")
    #expect(translator.translate(1_105, to: .thai).x == "หนึ่งพันหนึ่งร้อยห้า")
    #expect(translator.translate(1_110, to: .thai).x == "หนึ่งพันหนึ่งร้อยสิบ")
    #expect(translator.translate(1_111, to: .thai).x == "หนึ่งพันหนึ่งร้อยสิบเอ็ด")
    #expect(translator.translate(1_115, to: .thai).x == "หนึ่งพันหนึ่งร้อยสิบห้า")
    #expect(translator.translate(1_121, to: .thai).x == "หนึ่งพันหนึ่งร้อยยี่สิบเอ็ด")
    #expect(translator.translate(1_125, to: .thai).x == "หนึ่งพันหนึ่งร้อยยี่สิบห้า")
    #expect(translator.translate(3_005, to: .thai).x == "สามพันห้า")
    #expect(translator.translate(3_105, to: .thai).x == "สามพันหนึ่งร้อยห้า")
    #expect(translator.translate(3_155, to: .thai).x == "สามพันหนึ่งร้อยห้าสิบห้า")
    #expect(translator.translate(8_000, to: .thai).x == "แปดพัน")
    #expect(translator.translate(8_126, to: .thai).x == "แปดพันหนึ่งร้อยยี่สิบหก")
    #expect(translator.translate(10_000, to: .thai).x == "หนึ่งหมื่น")
    #expect(translator.translate(10_001, to: .thai).x == "หนึ่งหมื่นหนึ่ง")
    #expect(translator.translate(20_000, to: .thai).x == "สองหมื่น")
    #expect(translator.translate(20_001, to: .thai).x == "สองหมื่นหนึ่ง")
    #expect(translator.translate(20_010, to: .thai).x == "สองหมื่นสิบ")
    #expect(translator.translate(20_101, to: .thai).x == "สองหมื่นหนึ่งร้อยหนึ่ง")
    #expect(translator.translate(100_000, to: .thai).x == "หนึ่งแสน")
    #expect(translator.translate(100_001, to: .thai).x == "หนึ่งแสนหนึ่ง")
    #expect(translator.translate(200_000, to: .thai).x == "สองแสน")
    #expect(translator.translate(201_000, to: .thai).x == "สองแสนหนึ่งพัน")
    #expect(translator.translate(200_001, to: .thai).x == "สองแสนหนึ่ง")
    #expect(translator.translate(201_001, to: .thai).x == "สองแสนหนึ่งพันหนึ่ง")
    #expect(translator.translate(300_000, to: .thai).x == "สามแสน")
    #expect(translator.translate(300_451, to: .thai).x == "สามแสนสี่ร้อยห้าสิบเอ็ด")
    #expect(translator.translate(502_305, to: .thai).x == "ห้าแสนสองพันสามร้อยห้า")
    #expect(translator.translate(999_999, to: .thai).x == "เก้าแสนเก้าหมื่นเก้าพันเก้าร้อยเก้าสิบเก้า")
    #expect(translator.translate(1_000_000, to: .thai).x == "หนึ่งล้าน")
    #expect(translator.translate(21_700_098, to: .thai).x == "ยี่สิบเอ็ดล้านเจ็ดแสนเก้าสิบแปด")
    #expect(translator.translate(62_000_000, to: .thai).x == "หกสิบสองล้าน")
    #expect(translator.translate(62_003_005, to: .thai).x == "หกสิบสองล้านสามพันห้า")
    #expect(translator.translate(62_003_105, to: .thai).x == "หกสิบสองล้านสามพันหนึ่งร้อยห้า")
    #expect(translator.translate(62_003_155, to: .thai).x == "หกสิบสองล้านสามพันหนึ่งร้อยห้าสิบห้า")
    #expect(translator.translate(999_999_999, to: .thai).x == "เก้าร้อยเก้าสิบเก้าล้านเก้าแสนเก้าหมื่นเก้าพันเก้าร้อยเก้าสิบเก้า")
    #expect(translator.translate(99_000_000_909, to: .thai).x == "เก้าสิบเก้าพันล้านเก้าร้อยเก้า")
    #expect(translator.translate(99_999_999_999, to: .thai).x == "เก้าสิบเก้าพันล้านเก้าร้อยเก้าสิบเก้าล้านเก้าแสนเก้าหมื่นเก้าพันเก้าร้อยเก้าสิบเก้า")
    #expect(translator.translate(1_000_000_000_000, to: .thai).x == "หนึ่งล้านล้าน")
    #expect(translator.translate(100_000_000_000_000, to: .thai).x == "หนึ่งร้อยล้านล้าน")
    #expect(translator.translate(-1, to: .thai).x == "ลบหนึ่ง")

    let temp1 = "-1.5"
    #expect(translator.translate(temp1, to: .thai).x == "ลบหนึ่งจุดห้า")
    // "1.5" หนึ่งจุดห้า
    // "3.1415926" สามจุดหนึ่งสี่หนึ่งห้าเก้าสองหก

    let temp2 = "-3.1415926"
    #expect(translator.translate(temp2, to: .thai).x == "ลบสามจุดหนึ่งสี่หนึ่งห้าเก้าสองหก")
    // "3.4e13" สามจุดสี่เท่าสิบถึงพลังของสิบสาม
    // 8,126 — eight is in the thousand digit spot, so we read “eight” followed by the name of the thousand digit in Thai which is แปดพัน (bpàaet phan).
    // 8,126 — one is in the hundred digit spot, so we read “one” in Thai followed by the name of the hundred digit in Thai which is หนึ่งร้อย (nùeng ráauy).
    // 8,126 — two is in the ten digit spot, so we read “two” followed by the name of the ten digit in Thai which is ยี่สิบ (yîi sìp). (Don’t forget that for 20, Thai people read it ยี่สิบ [yîi sìp]).
    // 8,126 — six is in the unit digit spot, so we read “six” in Thai which is หก (hòk). (Don’t forget that for the unit digit, you read only the number and don’t read the name of the digit.)
    // 8,126 is แปดพันหนึ่งร้อยยี่สิบหก (bpàaet phan nùeng ráauy yîi sìp hòk).
    // 300,451 — three is in the hundred-thousand digit spot so we read “three” in Thai followed by the name of the hundred-thousand digit which is สามแสน (sǎam sǎaen).
    // 300,451 — since the number in the ten-thousand and thousand digit spots is 0, we can skip reading these two digits.
    // 300,451 — four is in the hundred digit spot, so we read “four” followed by the name of the hundred digit in Thai which is สี่ร้อย (sìi ráauy).
    // 300,451 — five is in the ten digit spot, so we read “five” followed by the name of the ten digit in Thai which is ห้าสิบ (hâa sìp).
    // 300,451 — one is in the unit digit spot, so we read one in Thai which is เอ็ด (èt). (Don’t forget that for numbers higher than 10, if 1 is in the unit digit spot, we read it เอ็ด [èt].)
    // 300,451 is สามแสนสี่ร้อยห้าสิบเอ็ด (sǎam sǎaen sìi ráauy hâa sìp èt).
    // Example of 21,700,098
    // 21 and 700,098 — you divide the number into parts for every six digits from right to left first, so you get 21 and 700,098.
    // 21,700,098 — you read the left part first followed by ล้าน (láan) which is ยี่สิบเอ็ดล้าน (yîi sìp èt láan).
    // 21,700,098 — you read the right part after that which is เจ็ดแสนเก้าสิบแปด (jèt sǎaen gâo sìp bpàaet).
    // 21,700,098 is ยี่สิบเอ็ดล้านเจ็ดแสนเก้าสิบแปด (yîi sìp èt láan jèt sǎaen gâo sìp bpàaet).
}
